# facial_utils.py
# Archivo temporal mientras implementamos el reconocimiento facial

def obtener_embedding(imagen_bytes):
    # Por ahora, devuelve un vector vacío
    return []

def comparar_embeddings(embedding1, embedding2):
    # Retorna False por defecto (no hay comparación real todavía)
    return False