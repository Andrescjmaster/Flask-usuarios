# --- Framework principal ---
flask==3.0.3
gunicorn==22.0.0

# --- PostgreSQL ---
psycopg[binary,pool]==3.2.3
SQLAlchemy==2.0.36
numpy==1.26.4
# Nota: Numpy se mantiene porque es utilizado por 'psycopg' indirectamente en el manejo de bytes, 
# y también se usa en app.py si decidiste no eliminar la línea 'import numpy as np'.

# --- Utilidades ---
python-dotenv==1.0.1